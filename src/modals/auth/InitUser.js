import React from "react";
import { useTranslation } from "react-i18next";

const InitUser = () => {
    const { t } = useTranslation();

    return <main className="modals-auth-init-user">init user</main>;
};

export default InitUser;
