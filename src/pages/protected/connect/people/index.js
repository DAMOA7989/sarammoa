import React from "react";
import { useTranslation } from "react-i18next";

const ConnectPeople = () => {
    const { t } = useTranslation();

    return (
        <main className="pages-protected-connect-people">connect people</main>
    );
};

export default ConnectPeople;
